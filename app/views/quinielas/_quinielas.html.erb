 <div class="page-header">
            <h1 id="tables">Fase de grupos</h1>
            </div>
            <%= form_for @quiniela, url: '/quinielas'  do |f| %>
               
                    <%= f.fields_for :bets do |builder| %>
                      <%= render :partial => 'bets_fields', :locals => {:f => builder} %>
                    <% end %>

              <p>
                <%= f.submit %>
              </p>
              <% end %>
