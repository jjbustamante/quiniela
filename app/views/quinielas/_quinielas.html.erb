


<%= form_for @quiniela, url: '/quinielas', :html => {:id => "quinielaForm"}  do |f| %>
            <div class="page-header">
            <h1 id="tables">Crea tu Quiniela </h1>
            </div>
               <h3 id="tables">Nombre:</h3><%= f.text_field :name, :class => "form-control required", :maxlength => "35" %>
               <div class="page-header">
            <h2 id="tables">Fase de grupos</h2>
            </div>

<script type="text/javascript">
 var teamsData = [];
 var firstRoundData = [];
  var qRoundData = [];
   var sRoundData = [];
    var fRoundData = [];
     var cRoundData = [];
 var resultsData  = [];                

</script>

                    <%= f.fields_for :bets do |builder| %>
                      <%= render :partial => 'bets_fields', :locals => {:f => builder} %>
                    <% end %>

                     

                 <%= render :partial => 'brackets_new' %>



              <p style="text-align:left;width:100%;margin-top:20px;">
                <%= f.submit "GUARDAR QUINIELA" ,:class => "btn btn-primary", :id => "saveQuinela"  %>

              </p>
              <% end %>
<script type="text/javascript">
jQuery.extend(jQuery.validator.messages, {
    required: "",
    digits: ""
    
});
  $( "#quinielaForm" ).validate();
</script>