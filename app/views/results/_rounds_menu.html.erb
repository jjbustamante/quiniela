 <div class="page-header">
              <h1 id="tables">Rondas</h1>
            </div>

            <div class="col-xs-12 col-sm-12 sidebar-offcanvas" id="sidebar" role="navigation">
          <div class="list-group">
                    <%
                @first =  true
                @rounds.each do |round| 
                  %>
              

                 <a href="#round<%= round.id%>" class="list-group-item <% if @first %>
active
<% end %> disabled"><%= round.name%></a>
    <% 
 @first =  false
    end %>

          </div>
        </div>

            
           
